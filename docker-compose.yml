version: '3.9'

services:
  ai-code-reviewer:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    ports:
      - "8000:8000"
